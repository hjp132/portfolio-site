<section class="section {{colour}}">
    <div class="container">
        <div class="container-left">
            <h2>{{title}}</h2>
            <div class="container-linebreak"></div>
            <p>{{text}}</p>
        </div>
    </div>
</section>